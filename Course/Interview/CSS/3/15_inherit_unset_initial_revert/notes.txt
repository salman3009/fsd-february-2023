unset : 
is an interesting property since it will either do the same thing as initial or inherit
initial:
The easiest CSS value to understand is initial. This value just resets the CSS property to its initial value.
rever:
evert is the finally piece of the puzzle when it comes to resetting CSS styles since revert allows you to reset a CSS value back to the browser specific style